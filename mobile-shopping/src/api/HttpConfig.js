//Cấu hình chung cho việc truyền tải HTTP sử dụng ajax

export const HTTP_CONFIG = {
    BASE_URL: "https://dummyjson.com",
    TIMEOUT: 3000,
    HEADERS: {
        'Content-Type': 'application/json',
    }
};